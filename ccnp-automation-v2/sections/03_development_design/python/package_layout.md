# Section 03 - Development and design checklist

- Create a package layout (`src/`, `tests/`, `pyproject.toml`).
- Add type hints and docstrings for public functions.
- Add `pytest` tests for parser and client helper functions.
- Add lint/format commands to CI.
